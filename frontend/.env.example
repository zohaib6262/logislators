# Backend API base URL. Must include /api (backend mounts routes under /api).
# Local dev (backend default port 8000):
VITE_BASE_URL=http://localhost:8000/api
# Production (use your backend host and port):
# VITE_BASE_URL=https://your-api.example.com/api
